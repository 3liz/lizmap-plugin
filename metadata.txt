; the next section is mandatory
[general]
name=lizmap
qgisMinimumVersion=1.6
description=Publication plugin for Lizmap Web Client, by 3liz.com
category=Web
version=version 1.6.0
; end of mandatory metadata

; start of optional metadata
changelog=
  Version 1.6.0
  * New : addition of more cache parameters : cache expiration and metatile
  * Enhancement : Heavy refactoring of the code ("data driven") to easy addition of new layers properties.

; tags are in comma separated value format, spaces are allowed
tags=lizmap, server, web, publication, client, html, 3liz, ftp

; these metadata can be empty
; in a future version of the web application it will
; be probably possible to create a project on redmine
; if they are not filled
homepage=http://hub.qgis.org/projects/lizmapplugin
tracker=http://hub.qgis.org/projects/lizmapplugin/issues
repository=https://bitbucket.org/mdouchin/lizmapplugin
icon=icon.png

; experimental flag
experimental=False

; deprecated flag (applies to the whole plugin and not only to the uploaded version)
deprecated=False
