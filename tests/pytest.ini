[pytest]
log_cli=1
log_cli_level=error
qgis_gui_enabled=false
# For some reason, callinf exitQgis()
# cause a segfault error when exiting 
# test session
qgis_disable_exit=true
norecursedirs=
    tests/data
    .local
    .venv
    .docker
    .ruff_cache
    *.egg-info
